% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dag_execute.R
\name{dag_execute}
\alias{dag_execute}
\alias{dag_execute,model_dag,list,logical-method}
\alias{dag_execute,model_dag,list,missing-method}
\title{Execute a DAG object}
\usage{
dag_execute(dag, nodes, verbose = TRUE)

\S4method{dag_execute}{model_dag,list,logical}(dag, nodes, verbose = TRUE)

\S4method{dag_execute}{model_dag,list,missing}(dag, nodes, verbose = TRUE)
}
\arguments{
\item{dag}{a model_dag object}

\item{nodes}{a named list of nodes (model_node, data_node, and prediction_node objects) where names are node IDs}

\item{verbose}{logical, whether to print progress information}
}
\value{
a list containing the results of each node execution

a list containing the results of each node execution
}
\description{
Executes a directed acyclic graph (DAG) object by traversing the graph
and executing nodes in the correct topological order.
}
\examples{
# Create a simple DAG
D = iris_DatasetExperiment()
M = example_model()

# Create nodes
data_node1 = data_node(name = 'Data', data = D)
model_node1 = model_node(name = 'Model', model = M)

# Create DAG
dag = model_dag(
    name = 'Simple DAG',
    description = 'A simple example DAG',
    edges = list(
        list(from = 'Data', to = 'Model')
    )
)

# Execute DAG
nodes = list(Data = data_node1, Model = model_node1)
results = dag_execute(dag, nodes)
}
